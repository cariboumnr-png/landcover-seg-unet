name: demo_data
input_dirpath: ${exp_root}/input/${input_data.name}
filenames:
  fit_image: image.tif
  fit_label: label.tif
  test_image: image.tif
  test_label: label.tif
  config: config.json
dirs:
  fit: ${exp_root}/input/${input_data.name}/fit
  test: ${exp_root}/input/${input_data.name}/test
  config: ${exp_root}/input/${input_data.name}/config
filepaths:
  fit_image: ${input_data.dirs.fit}/${input_data.filenames.fit_image}
  fit_label: ${input_data.dirs.fit}/${input_data.filenames.fit_label}
  test_image: ${input_data.dirs.test}/${input_data.filenames.test_image}
  test_label: ${input_data.dirs.test}/${input_data.filenames.test_label}
  config: ${input_data.dirs.config}/${input_data.filenames.config}