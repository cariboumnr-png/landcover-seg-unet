schedule:
  max_epoch: 50
  max_step: 1000000
  log_loss_every: 50
  val_every: 1
  infer_every: 1
  ckpt_every: 5

monitor:
  metric_name: iou
  track_heads: [base]
  track_mode: max
  allow_early_stop: True
  patience: 10
  min_delta: 0.0005

precision:
  use_amp: True

optimization:
  grad_clip_norm: 1.0

logit_adjust:
  enable: True
  alpha: 1.0
