# Main entry to the config tree
defaults:
  - _self_
  - components: default
  - runtime: default
  - phases: default

phases_schema: ${session.phases_schema}
resume_from_last: False
train_mode: epochs