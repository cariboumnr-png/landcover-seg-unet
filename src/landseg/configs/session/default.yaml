# Main entry to the config tree
defaults:
  - _self_
  - data_loader: default
  - engine_exec: default
  - engine_optim: default
  - engine_tasks: default
  - orchestration: default

mode: continuous # continuous | curriculum