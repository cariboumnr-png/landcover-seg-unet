# Config for training phases
# experiment config (consumed by controller)
experiment:
  ckpt_dpath: ./checkpoints
  preview_dpath: ./previews

phases:
  # each phase
    - name: Phase 1
      num_epochs: 50
      active_heads: [layer1]
      masked_classes:
        layer1: []
      frozen_heads: null
      lr_scale: 1.0