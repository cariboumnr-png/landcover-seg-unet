name: demo_data
input_dirpath: ${exp_root}/input/${inputs.data.name}
filenames:
  dev_image: image.tif
  dev_label: label.tif
  test_image: image.tif
  test_label: label.tif
  config: config.json
dirs:
  dev: ${exp_root}/input/${inputs.data.name}/dev
  test: ${exp_root}/input/${inputs.data.name}/test
  config: ${exp_root}/input/${inputs.data.name}/config
filepaths:
  dev_image: ${inputs.data.dirs.dev}/${inputs.data.filenames.dev_image}
  dev_label: ${inputs.data.dirs.dev}/${inputs.data.filenames.dev_label}
  test_image: ${inputs.data.dirs.test}/${inputs.data.filenames.test_image}
  test_label: ${inputs.data.dirs.test}/${inputs.data.filenames.test_label}
  config: ${inputs.data.dirs.config}/${inputs.data.filenames.config}